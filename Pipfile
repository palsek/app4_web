[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
psycopg2-binary = ">=2.8"
djangorestframework = ">=1.0"
requests = ">=1.0"
Django = ">=3.0,<4.0"

[requires]
python_version = "3.7"
